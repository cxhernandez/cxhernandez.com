name: cv_pdf
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  # PDF generation (generate_cv_pdf.py)
  - weasyprint
  - markdown
  # Google Scholar crawler (gscrawler.py)
  - pandas
  - scholarly
  - free-proxy  # Required for FreeProxies

name: cv_pdf
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  # PDF generation (generate_cv_pdf.py)
  - weasyprint
  - markdown
  # Google Scholar crawler (gscrawler.py)
  - pandas
  - scholarly>=1.7.12  # Requires version with proxy fix (PR #579)

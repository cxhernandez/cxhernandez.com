name = "photography-store-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Note: The worker currently only provides a /health endpoint.
# The storefront uses pre-built Square payment links and static inventory.json.
# If dynamic catalog/checkout is needed in the future, add secrets:
#   wrangler secret put SQUARE_ACCESS_TOKEN
#   wrangler secret put SQUARE_LOCATION_ID
